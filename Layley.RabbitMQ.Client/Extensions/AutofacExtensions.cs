namespace Layley.RabbitMQ.Client.Extensions
{
    public static class AutofacExtensions
    {
        //public static void RegisterRabbitMQClient(this ILifetimeScope container) =>
        //    container.Update(builder =>
        //    {
        //        builder.RegisterType<RabbitConnectionFactory, IRabbitConnectionFactory>();
        //        builder.RegisterType<RabbitPublisher, IRabbitPublisher>();
        //    });
    }
}
